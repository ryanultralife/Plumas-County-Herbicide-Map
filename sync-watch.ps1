# DEPRECATED. Auto-sync is now a batch file.
# Use sync.bat (which runs the sync.dat engine). See sync.dat for config + usage.
Write-Host "Deprecated - run sync.bat instead (engine + config live in sync.dat)."
